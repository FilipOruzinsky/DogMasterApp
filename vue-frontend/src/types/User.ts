export interface User{ // neviem ci tu stacia len tie data ktore sa vyuziju pri logine ?
    userID: string
    userName: string
    firstName: string
    lastName: string
    address: string
    phoneNumber: string
    email: string
}
